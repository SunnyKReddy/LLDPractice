package strategies;

import models.ParkingSpot;
import models.VehicleType;
import repositories.ParkingSpotRepository;
import strategies.parkingspotallocationstrategy.ParkingSpotAllocation;

public class BikeParkingSpotAllocation  implements ParkingSpotAllocation {
    @Override
    public ParkingSpot allocateAvailableParkingSpot(VehicleType vehicleType) {
        //ParkingSpot parkingSpot = ParkingSpotRepository.parkingSpotRepository.
        for (int i = 101; i <= 111; i++) {
            
        }
        return null;
    }
}
